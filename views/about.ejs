<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>About | User Profile</title>
    <style>
      /* Custom CSS for profile image */
      .profile-img {
        width: 120px; /* Slightly larger size */
        height: 120px;
        border-radius: 50%;
        object-fit: cover;
        margin-bottom: 1rem;
        border: 3px solid #f39c12; /* Add a border for a polished look */
      }
      .welcome-heading {
        font-family: 'Kaushan Script', cursive;
        font-size: 2rem;
        color: #f39c12; /* Use a standout color */
        margin-bottom: 1.5rem;
      }

    </style>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <% if (user) { %>
    <div class="container mt-3">
      <div class="profile-container text-center">
        <h2 class="welcome-heading">Welcome, <%= user.firstName %> <%= user.lastName %>!</h2>
        <img src="<%= user.profileImageURL %>" alt="Profile Image" class="profile-img img-fluid" />
        
        <div class="user-details mt-2">
          <p class="user-info"><strong>First Name:</strong> <%= user.firstName %></p>
          <p class="user-info"><strong>Last Name:</strong> <%= user.lastName %></p>
          <p class="user-info"><strong>Email:</strong> <%= user.email %></p>
          <p class="user-info"><strong>Password:</strong> <em>Encrypted and secured</em></p>
        </div>
        
        <div class="mt-3">
          <a href="/user/editUser/<%= user._id %>" class="btn btn-custom btn-sm mx-2">Edit Profile</a>
          <a href="/user/changePassword/<%= user._id %>" class="btn btn-custom btn-sm mx-2">Change Password</a>
        </div>
      </div>
    </div>
    <% } else { %>
      <div class="container mt-5 text-center">
        <p>You need to be logged in to view this page. Please <a href="/user/signin" class="btn btn-outline-primary">sign in</a>.</p>
      </div>
    <% } %>

    <%- include('./partials/script') %>
  </body>
</html>
